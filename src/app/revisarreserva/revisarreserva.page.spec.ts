import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RevisarreservaPage } from './revisarreserva.page';

describe('RevisarreservaPage', () => {
  let component: RevisarreservaPage;
  let fixture: ComponentFixture<RevisarreservaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RevisarreservaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
